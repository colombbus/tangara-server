<table class="table table-condensed table-hover">
    <tr> 
        <th>{{ 'admin.project'|trans }}</th>
        <th>{{ 'admin.owner'|trans }}</th>
        <th>{{ 'admin.project_datetime'|trans }}</th>
    </tr>
    {% for key, project in projects %}
    <tr>
        <td><a href="{{ path('tangara_project_show', {'projectId': project.id}) }}"> {{ project.name }}</a></td>
        <td><a href="{{ path('tangara_user_profile_show', {'user_id': project.owner.id}) }}">{{ project.owner }}</a></td>
        <td>{{ project.created|date("m/d/Y") }}</td>
    </tr>
    {% endfor %}
</table>
<div id="pagination">
    {{ knp_pagination_render(projects) }}
</div>